---
title: "test"
output: html_document
---
```{r}
library(tidyverse)
library(spotifyr)
```
```{r}
lig <- get_playlist_audio_features("", "37i9dQZF1DX8C9xQcOrE6T")
twooo <- get_playlist_audio_features("", "6dv5FbNRVlQelteqnNDFTe")
seventies <- get_playlist_audio_features("", "67zYW4wCQezR6AH8EuNZ1Q")
```

```{r}
grammy <- get_playlist_audio_features("", "4kQovkgBZTd8h2HCM3fF31")
edison <- get_playlist_audio_features("", "37i9dQZF1DX8mnKbIkppDf")
```

```{r}
combo <-
  bind_rows(
    seventies %>% mutate(category = "70s"),
    twooo %>% mutate(category = "2000s")
  )
```



```{r}
combo %>%
  ggplot(aes(x = danceability)) +
  geom_histogram(binwidth = 0.1) +
  facet_wrap(~category)
combo %>%
  ggplot(aes(x = category, y = loudness)) +
  geom_boxplot()
combo %>%
  ggplot(aes(x = category, y = loudness)) +
  geom_violin()
combo %>%
  ggplot(aes(x = category, y = danceability)) +
  geom_boxplot()
combo %>%
  ggplot(aes(x = category, y = danceability)) +
  geom_violin()
combo %>%
  ggplot(aes(x = category, y = energy)) +
  geom_violin()

```

```{r}
grammy %>% ggplot(aes(x = valence, y = energy)) + geom_point() + geom_smooth()
grammy %>% ggplot(aes(x = valence, y = energy)) + geom_jitter() + geom_smooth()
```

```{r}
awards %>%                    # Start with awards.
  mutate(
    mode = ifelse(mode == 0, "Minor", "Major")
  ) %>%
  ggplot(                     # Set up the plot.
    aes(
      x = valence,
      y = energy,
      size = loudness,
      colour = mode
    )
  ) +
  geom_point() +              # Scatter plot.
  geom_rug(size = 0.1) +      # Add 'fringes' to show data distribution.
  geom_text(                  # Add text labels from above.
    aes(
      x = valence,
      y = energy,
      label = label
    ),
    data = 
      tibble(
        label = c("Altijd wel iemand", "ENERGY"),
        category = c("Edisons", "Grammys"),
        valence = c(0.090, 0.123),
        energy = c(0.101, 0.967)
      ),
    colour = "black",         # Override colour (not mode here).
    size = 3,                 # Override size (not loudness here).
    hjust = "left",           # Align left side of label with the point.
    vjust = "bottom",         # Align bottom of label with the point.
    nudge_x = -0.05,          # Nudge the label slightly left.
    nudge_y = 0.02            # Nudge the label slightly up.
  ) +
  facet_wrap(~category) +     # Separate charts per playlist.
  scale_x_continuous(         # Fine-tune the x axis.
    limits = c(0, 1),
    breaks = c(0, 0.50, 1),   # Use grid-lines for quadrants only.
    minor_breaks = NULL       # Remove 'minor' grid-lines.
  ) +
  scale_y_continuous(         # Fine-tune the y axis in the same way.
    limits = c(0, 1),
    breaks = c(0, 0.50, 1),
    minor_breaks = NULL
  ) +
  scale_colour_brewer(        # Use the Color Brewer to choose a palette.
    type = "qual",            # Qualitative set.
    palette = "Paired"        # Name of the palette is 'Paired'.
  ) +
  scale_size_continuous(      # Fine-tune the sizes of each point.
    trans = "exp",            # Use an exp transformation to emphasise loud.
    guide = "none"            # Remove the legend for size.
  ) +
  theme_light() +             # Use a simpler theme.
  labs(                       # Make the titles nice.
    x = "Valence",
    y = "Energy",
    colour = "Mode"
  )
```








OPSLAG

HISTS:
```{r}
seventotwenty %>%
  ggplot(aes(x = key)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = mode)) +
  geom_histogram(binwidth = 0.1) +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = valence)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = loudness)) +
  geom_histogram(binwidth = 0.1) +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = energy)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = tempo)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = instrumentalness)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

seventotwenty %>%
  ggplot(aes(x = danceability)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

sebas %>%
  ggplot(aes(x = key)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()

seventotwenty %>%
  ggplot(aes(x = mode)) +
  geom_histogram() 

sebas %>%
  ggplot(aes(x = valence)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()

sebas %>%
  ggplot(aes(x = loudness)) +
  geom_histogram(binwidth = 0.1) 


sebas %>%
  ggplot(aes(x = energy)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()


sebas %>%
  ggplot(aes(x = tempo)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()
  

sebas %>%
  ggplot(aes(x = instrumentalness)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()


sebas %>%
  ggplot(aes(x = danceability)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()
```



RANDOM SHIT
```{r}
seventotwenty %>%
  ggplot(aes(x = valence)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

sebas %>%
  ggplot(aes(x = valence)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()
  
seventotwenty %>%
  ggplot(aes(x = tempo)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

sebas %>%
  ggplot(aes(x = tempo)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10()

seventotwenty %>%
  ggplot(aes(x = valence)) +
  geom_histogram(binwidth = 0.1) +
  scale_x_log10() +
  facet_wrap(~category)

sebas %>%
  ggplot(aes(y = tempo,x = time_signature)) +
  geom_violin() 
seventotwenty %>%
  ggplot(aes(x = category, y = tempo)) +
  geom_violin() +
  scale_y_log10()


seventotwenty %>%
  ggplot(aes(x = valence, y = energy)) +
  geom_point(binwidth = 0.1) 
```

```{r}
sebas %>% 
  ggplot(aes(x = added_at, y = key)) +
  geom_point()

seb2017 %>%
  ggplot(aes(x = loudness, y = tempo)) +
  geom_point()  

seb2018 %>%
  ggplot(aes(x = loudness, y = tempo)) +
  geom_point()  

seb2019 %>%
  ggplot(aes(x = loudness, y = tempo)) +
  geom_point()  

seb2020 %>%
  ggplot(aes(x = loudness, y = tempo)) +
  geom_point()  

seventotwenty %>%
  ggplot(aes(x = category, y = key)) +
  geom_violin() +
  scale_y_log10()


```